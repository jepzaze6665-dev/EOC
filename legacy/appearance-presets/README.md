# Appearance presets (archived 2026-09-25)

Six faces the player could pick separately from the class. Replaced by
"Class = Preset Character" (brief 03, section 10): each class now has one fixed look,
weapon, animation set and VFX theme (`preset` in `client/data/classes.js`).
Random character names moved to `client/data/names.js`.
